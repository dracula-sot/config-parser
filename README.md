# Config Parser Fix

Small utility to normalize malformed configuration files in legacy systems.

## Features
- Handles invalid key formats
- Normalizes values before parsing
- Lightweight and easy to integrate

## Notes
This was built after running into multiple parsing issues in production environments.
